export type MaskedInputProps = {
  /**
   * A string that defines the input mask pattern.
   */
  mask: string
}

export type WithMaskOptions = {
  /**
   * A string that defines the input mask pattern.
   */
  mask: string
}
